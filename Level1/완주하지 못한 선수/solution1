sort() : 배열 요소를 순서대로 정렬해주는 함수
participant = ["leo", "kiki", "eden"]
participant.sort() => ["eden", "kiki", "leo"]

zip() : 각 배열의 요소를 짝지어주는 함수
participant3 = ["mislav", "stanko", "mislav", "ana"]
completion3 = ["stanko", "ana", "mislav"]

zip(participant3, completion3)
=>
ana ana
mislav mislav
mislav stanko
mislav

pop() : 배열의 마지막 요소를 반환하는 함수


[결과]
1. pop() 함수를 사용해서 participant의 마지막 요소를 반환함
2. (동명이인이 있는 경우) zip 함수를 이용해 두 요소가 다르다면
    p(첫 번째 요소)를 반환함